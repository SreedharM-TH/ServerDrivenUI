import SwiftUI

enum AppFonts {
    static let title = Font.title2.weight(.bold)
    static let label = Font.subheadline.weight(.semibold)
    static let body = Font.body
    static let caption = Font.caption
    static let counter = Font.caption.monospacedDigit()
}
